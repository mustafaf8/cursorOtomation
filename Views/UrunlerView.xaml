<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="SolarAutomation.Views.UrunlerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Ürün Ekleme Formu -->
            <GroupBox Header="Yeni Ürün Ekle" Grid.Row="0" Margin="0,0,0,20">
                <Grid Margin="10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- İlk Satır -->
                    <Label Content="Ürün Adı:" Grid.Row="0" Grid.Column="0" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtUrunAdi" Grid.Row="0" Grid.Column="1" Margin="0,0,20,10"/>

                    <Label Content="Marka:" Grid.Row="0" Grid.Column="2" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtMarka" Grid.Row="0" Grid.Column="3" Margin="0,0,20,10"/>

                    <Label Content="Model:" Grid.Row="0" Grid.Column="4" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtModel" Grid.Row="0" Grid.Column="5" Margin="0,0,20,10"/>

                    <!-- İkinci Satır -->
                    <Label Content="Fiyat:" Grid.Row="1" Grid.Column="0" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtFiyat" Grid.Row="1" Grid.Column="1" Margin="0,0,20,10"/>

                    <Label Content="Stok:" Grid.Row="1" Grid.Column="2" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtStok" Grid.Row="1" Grid.Column="3" Margin="0,0,20,10"/>

                    <Label Content="Birim:" Grid.Row="1" Grid.Column="4" Margin="0,0,10,10"/>
                    <TextBox x:Name="txtBirim" Grid.Row="1" Grid.Column="5" Margin="0,0,20,10"/>

                    <!-- Ekle Butonu -->
                    <Button Content="Ekle" 
                            Grid.Row="1" 
                            Grid.Column="6" 
                            Width="80" 
                            Height="30"
                            Margin="10,0,0,0"
                            Click="Ekle_Click"/>
                </Grid>
            </GroupBox>

            <!-- Ürün Listesi -->
            <DataGrid x:Name="dgUrunler" 
                      Grid.Row="1" 
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      Margin="0,0,0,20">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Ürün Adı" Binding="{Binding UrunAdi}" Width="150"/>
                    <DataGridTextColumn Header="Marka" Binding="{Binding Marka}" Width="150"/>
                    <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="150"/>
                    <DataGridTextColumn Header="Fiyat" Binding="{Binding Fiyat}" Width="150"/>
                    <DataGridTextColumn Header="Stok" Binding="{Binding Stok}" Width="150"/>
                    <DataGridTextColumn Header="Birim" Binding="{Binding Birim}" Width="150"/>
                    <DataGridTemplateColumn Header="İşlem" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Sil" 
                                        Click="Sil_Click"
                                        Background="{StaticResource TextSecondaryColor}"
                                        Foreground="White"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>

            <!-- Toplam -->
            <StackPanel Grid.Row="2" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right"
                        Margin="0,0,0,0">
                <TextBlock Text="Toplam Ürün Sayısı: " 
                           FontSize="16" 
                           FontWeight="Medium"
                           VerticalAlignment="Center"/>
                <TextBlock x:Name="txtToplamUrun" 
                           Text="0" 
                           FontSize="16" 
                           FontWeight="Bold"
                           Margin="5,0,0,0"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl> 