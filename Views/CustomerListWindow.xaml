<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="SolarAutomation.Views.CustomerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Müşteri Listesi" Height="600" Width="1000"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Arama ve Filtreleme -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="txtSearch" Width="200" Height="25" Margin="0,0,10,0"
                     TextChanged="TxtSearch_TextChanged" 
                     ToolTip="Müşteri adı veya telefon numarası ile arama yapın"/>
            <Button Content="Yeni Müşteri" Width="100" Height="25" Click="BtnNewCustomer_Click"/>
        </StackPanel>

        <!-- Müşteri Listesi -->
        <DataGrid x:Name="dgCustomers" Grid.Row="1" AutoGenerateColumns="False" 
                  IsReadOnly="True" SelectionMode="Single" 
                  MouseDoubleClick="DgCustomers_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="İsim" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Telefon" Binding="{Binding Phone}" Width="120"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="Adres" Binding="{Binding Address}" Width="250"/>
                <DataGridTextColumn Header="Kayıt Tarihi" Binding="{Binding CreatedDate, StringFormat={}{0:dd.MM.yyyy}}" Width="100"/>
                <DataGridTemplateColumn Header="İşlemler" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Düzenle" Margin="0,0,5,0" Click="BtnEdit_Click"/>
                                <Button Content="Sil" Click="BtnDelete_Click"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window> 